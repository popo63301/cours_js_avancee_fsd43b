// function add(a, b) {
//   return a + b;
// }

// (arguments) => valeur retournée

// const add = (a, b) => a + b;

// console.log(add(1, 5));

// // const generateObj = (name) => ({id: name})
// const generateObj = (name, age) => ({ name, age });

// console.log(generateObj("toto", 10));

//----------------

setTimeout(() => console.log("toto"), 2000);
